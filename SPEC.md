# Spec: boilerplate-testing

> Patterns and utilities for testing TypeScript full-stack apps. Spec-driven.

## Phase 0 — Green Baseline (blocks all feature work)
- [x] Verify every dependency version actually exists on the registry and fix the ones that do not, then commit a lockfile — 31 of 34 ranges resolved; `@storybook/addon-essentials`, `@storybook/addon-interactions` and `@storybook/test` were all removed in Storybook 9 and had no stable 9.x, so `pnpm install` failed outright (PR #22)
- [x] Get `install`, `typecheck`, `lint`, `test`, and `build` all passing locally from a clean clone — typecheck had 15 errors behind a `baseUrl` abort, 23 of 276 tests failed, `eslint` was neither installed nor configured, and no `build` script existed (PR #22)
- [x] Promote `workflow-templates/ci.yml` to `.github/workflows/ci.yml` and confirm it runs green on a PR — green on run #1 (PR #22)
- [x] Add a CI job matrix covering the supported Node version and fail the build on any warning — `engines.node` is `^22.13.0 || ^24.0.0` (eslint 10's floor, stricter than vite's `>=22.12.0`) and the matrix mirrors it; `engine-strict`, `strict-dep-builds`, `--strict-peer-dependencies`, `--frozen-lockfile`, `--max-warnings 0` and per-step `--throw-deprecation` all make warnings fatal; green on run #1 on both legs (PR #23)
- [x] Bump the pinned Actions to their Node 24 majors — the runner emits `##[warning] Node.js 20 is deprecated` for `actions/checkout@v4`, `actions/setup-node@v4`, `actions/upload-artifact@v4` and `pnpm/action-setup@v4`, the one warning the item-4 gates cannot reach — checkout/setup-node/upload-artifact → v7, download-artifact → v8, `pnpm/action-setup` → v6, `gitleaks/gitleaks-action` → v3, each target picked by reading `runs.using` in that action's own `action.yml` at the tagged major; `codecov/codecov-action` stays at v5 (composite, no Node runtime). `workflow-templates/actionPins.{ts,test.ts}` now audits every `uses:` line in `.github/workflows/` and `workflow-templates/` against a floor table, so the drift fails `pnpm test` instead of only showing up in a runner log. Zero `##[warning]` lines in the merged run (PR #24)
- [x] Support Node 26: `supertest/createTestApp.test.ts` "builder chaining preserves immutability across multiple calls" fails with `read ECONNRESET` on Node 26.5.1 and passes on 22 and 24 — the race was supertest's, not Node's: it binds the server itself on the first request that finds it unbound and closes it again when *that* request ends, resetting siblings still in flight. `createTestApp` (and `createNestTestApp`) now bind up front so supertest never takes ownership. `engines.node` is `^22.13.0 || ^24.0.0 || ^26.0.0` and the matrix mirrors it (PR #25)
- [x] Run the `Build` CI step under `NODE_OPTIONS=--throw-deprecation` like the other gates — two deprecations were in the way. DEP0190 (Storybook 9.1.20 shelling out through execa with `shell: true` plus an argument array in `extractStorybookMetadata`) is gone with the upgrade to Storybook 10.5.5; DEP0205 (`importModule` registering the TypeScript config loader through `module.register()`), which only surfaced underneath it on Node 26, is cleared by `patches/storybook@10.5.5.patch` forward-porting the unmerged upstream fix storybookjs/storybook#35337. Dropping Node 26 from the matrix would also have made this green and was rejected. `workflow-templates/gateSteps.ts` audits that every gate keeps its `NODE_OPTIONS` block and `workflow-templates/patchedDeps.ts` audits the patch pin, so neither can go missing quietly. Green on all three legs (PR #26)

Phase 0 items 1-3 complete as of PR #22 (2026-07-31): install (frozen lockfile),
typecheck, lint (0 errors, 0 warnings), 292 unit tests across 11 files, and
build all green in CI on Node 22.

Items 1-3 landed as one PR because they are circularly dependent — CI cannot be
green before the gates pass, the gates cannot be observed before install works,
and nothing may merge before CI exists. Three earlier runs each did item 1 alone
and correctly stopped at the unmergeable draft stage (PRs #20, #21, #22).

The ignored-build-scripts warning flagged against item 4 (`@swc/core`,
`esbuild`, `msw`) is resolved: all three are triaged into
`pnpm.onlyBuiltDependencies`, and `.npmrc` sets `strict-dep-builds=true` so the
next dependency that ships a postinstall fails the install instead of adding a
line to the warning list.

Item 5 closed the last warning class the item-4 gates could not reach. The
`Node.js 20 is deprecated` notice is emitted by the *runner*, about the
workflow, before any step of ours executes, so no gate running inside the job
can observe it — which is why it survived item 4 despite that item making every
other warning fatal. The pins now sit on majors whose `action.yml` declares
`runs.using: node24`, and `workflow-templates/actionPins.ts` turns the audit
into a unit test: it parses every `uses:` line under `.github/workflows/` and
`workflow-templates/` and checks the pinned major against a recorded floor. An
action absent from that floor table fails rather than passes, so adding one
forces its runtime to be classified. The merged run emitted no `##[warning]`
lines at all.

Node 26 is the reason item 6 existed rather than being folded into the item 4
matrix. The failure reproduced in five lines with no code from this repo — two
concurrent requests through one `supertest.agent(server)` against a
non-listening `http.Server` — and the predicted fix was the right one, though
the diagnosis was not: it is not Node server-lifecycle behaviour that changed
between 24 and 26. `supertest@7.2.2` binds the server itself on the first
request that finds it unbound (`lib/test.js#serverAddress`) and closes that
same server when *that* request ends (`#end`), so the first response tears the
listener down under a sibling's open socket. Node 26 only changed the timing
that used to hide the reset.

`createTestApp` and `createNestTestApp` therefore bind to an ephemeral port up
front, so `address()` is never null and supertest never assigns `this._server`.
Lifetime moved to `TestApp.close()` — one listener per TestApp instead of one
per request. `listen()` takes no host argument on purpose: passing one routes
through `dns.lookup()`, which would make `createTestApp` async. As predicted,
this changes the documented contract ("the server does **not** need to be
listening"); a caller-bound server is reused as-is. Node 26 is now in
`engines.node` and in the CI matrix, so the regression is caught by the gates.

Item 7 exists because `--throw-deprecation` is enforced on typecheck, lint and
test but not on build. Storybook 9.1.20 counts portable-stories files by
shelling out through execa with `shell: true` plus an argument array, which is
DEP0190 on Node 24+; it runs during post-build metadata extraction, so
`storybook build` exits 7 with `storybook-static/` already complete. It is not
reachable from first-party code, `--disable-telemetry` does not skip it (the
metadata is computed for the build, not only for telemetry), and Storybook's
own cache hides it on a warm `node_modules` — which is why it shows up only on
a clean install, i.e. only in CI. Options are a Storybook upgrade once upstream
stops passing an args array with `shell: true`, or dropping the portable-stories
count. Both are larger than a CI change.

The upgrade option is now available and DEP0190 is gone: Storybook 10.5.5 calls
`execCommandCountLines('git', ['grep', …])` with no `shell` option at all, and
the build satisfies `--throw-deprecation` on Node 22 and 24 — verified locally
on both, with `node_modules/.cache/storybook` cleared first so the 24h
`runTelemetryOperation` cache could not mask the call (the cache is written
with `{"key":"portableStories"}`, so its presence afterwards proves the path
ran).

Clearing DEP0190 exposed a second, unrelated deprecation on the Node 26 leg,
which the flag then correctly surfaced: DEP0205, `module.register()` is
deprecated, use `module.registerHooks()`. Storybook
raises it in `importModule` (`src/shared/utils/module.ts`), which registers a
TypeScript loader hook unconditionally on the first config import — before any
config is read, so it is not avoidable by writing `.storybook/main` as
JavaScript, and it is not first-party. It is still present in `storybook@next`
(10.6.0-alpha.3), so there is no released or prereleased version to move to.
DEP0205 does not exist on Node 22 or 24, which is why the upgrade looks clean
on two of the three legs.

Dropping Node 26 from the matrix would have closed item 7 and was rejected:
`engines.node` declares 26 as supported, so excluding the leg would weaken the
gate rather than satisfy it. The upstream fix exists but is unmerged —
storybookjs/storybook#35337, open since 2026-07-01 against `next` — so item 7
is closed by forward-porting it as `patches/storybook@10.5.5.patch`: prefer
`module.registerHooks()` with a synchronous esbuild loader (`loadSync`) where
that API exists, fall back to `module.register()` on runtimes without it
(Node 22.13–22.14, still inside `engines.node`). Every leg of the matrix takes
the `registerHooks` path, so a break in it fails all three rather than only the
newest.

The patch is pinned to an exact version in `pnpm.patchedDependencies`, which
makes a Storybook bump fail the install instead of resolving a version the
patch was never written against, and `workflow-templates/patchedDeps.ts` audits
the pin as a unit test — a patch that is undocumented, unpinned, missing from
disk, or drifted off the installed version fails `pnpm test` in a second rather
than failing one CI leg three minutes later inside `node_modules`. Delete the
patch, its pin, its `PATCH_REASONS` entry and the README note together when the
upstream fix ships; the audit fails on any of them left behind.

Known gaps still carried forward: Playwright E2E is not wired into CI — the
specs target a running app at `PLAYWRIGHT_BASE_URL` and this repo ships none, so
the template's e2e matrix was deliberately left out of the promoted workflow.
Prettier is not gated; there is no `format:check` script.

## Phase 1 — Unit Testing (Jest + Vitest)
- [x] Vitest config with coverage (v8), jsdom environment, path aliases
- [x] Jest config for Node.js backend testing (NestJS / Express)
- [x] Custom matchers: `toMatchResponse`, `toBeValidJwt`, `toBeISO8601`
- [x] Async test utilities: `waitForCondition`, `flushPromises`, `eventually`

## Phase 2 — Component Testing
- [x] Testing Library patterns: render helpers, custom `renderWithProviders`
- [x] MSW 2 handler library: auth, users, pagination
- [x] Storybook 9 interaction tests
- [x] Accessibility: `@axe-core/react` + automated WCAG checks

## Phase 3 — E2E Testing (Playwright)
- [x] Playwright config: multi-browser, CI mode, trace on failure
- [x] Auth fixture: `test.extend` with pre-authenticated `page`
- [x] Page Object Model: `LoginPage`, `DashboardPage`, `FormPage`
- [x] Visual regression: `expect(page).toHaveScreenshot()`

## Phase 4 — API Testing
- [x] Supertest helpers for Express/NestJS: `createTestApp`, typed request builder
- [x] Prisma test isolation: `beforeEach` truncate via `$transaction`
- [x] Seed factories with `@faker-js/faker` + `prisma-factory`
- [x] Contract testing example with Pact

## Phase 5 — Performance & Load
- [x] k6 load test script template (ramp-up, steady, ramp-down)
- [x] Lighthouse CI integration with budget assertions
- [x] Bundle size regression check (bundlesize)

## Phase 6 — CI Patterns
- [x] GitHub Actions: parallel matrix for unit / e2e / coverage
- [x] Flaky test retry config + quarantine strategy
- [x] Codecov integration + coverage badge

## Phase 7 — TDD Discipline
- [x] TDD kata series: FizzBuzz, Bowling, Gilded Rose — one commit per red/green/refactor step — 29 step commits, each verified to be the colour it claims by running the suite before committing. A fourth phase, `pin`, was needed: the Gilded Rose starts from inherited code, and a characterisation test cannot be red without lying, so `pin` steps are allowed only before the first `red`. Because the repo squash-merges, the history is also stored as data in `tdd/katas.ts` and audited by `tdd/steps.ts` + `tdd/katas.test.ts` — every claimed test must exist on disk, every test on disk must be claimed by one step, and the phase sequence must be a legal cycle, so the record fails `pnpm test` rather than rotting (PR #27)
- [x] Outside-in (London school) vs classicist TDD compared on the same feature — one order-placing feature built twice under `tdd/schools/`, both held to a shared contract (`orderContract.ts`, 14 behaviours per school) so the comparison is falsifiable rather than two different programs. The London design ends where driving purely from the outside actually ends: six ports, no implementation of any of them, and a suite that asserts call order and counts. The classicist design is Money/Catalogue/Promotions/Inventory/pricing tested directly, assembled last, with one fake at the payment boundary. Each suite turns out to be silent exactly where its design has no code — only the mockist tests can state "reserve before charge" (a state snapshot cannot tell it from the reverse), only the classicist tests can state "never oversell, never half-reserve" and the instant a promo dies. `design.test.ts` derives the README's structural claims from the wiring the contract actually runs, so a seventh port fails `pnpm test` instead of leaving a wrong sentence in a README. 92 new tests; green on Node 22, 24 and 26 on run #1 (PR #28)
- [x] Test-double taxonomy: dummy, stub, spy, mock, fake — with a when-to-use guide — one feature (`tdd/doubles/registerUser.ts`, four collaborators) with all five kinds demonstrated against it, so every "use a stub here" is a decision somebody could have made differently. The guide's central claim — each kind sees a different class of defect — is measured, not asserted: `faults.ts` injects five bugs one at a time and `detection.test.ts` runs all five probes against all five broken systems, so the README's matrix is derived by running the code. Four of the five faults are caught by exactly one kind; the fifth by two, and the mock's detection set turns out to be a strict superset of the spy's — asserted as the matrix's only domination rather than buried, with the README arguing the half no test can settle (the same strictness breaks on changes that broke nothing). The probes live in `probes.ts` so the test a reader sees per kind is the code the matrix is measured from, and each is first shown green against the correct system. The fake is held to `userStoreContract.ts`, run against a store with one rule dropped to prove the contract has teeth; the audit log is a dummy on the self-service path and a spy on the admin path, because the kind is a property of the test, not the object. `taxonomy.test.ts` checks the README's headings, guidance sentences, module links and every matrix cell against `taxonomy.ts`. 42 new tests; green on Node 22, 24 and 26 on run #1 (PR #29)
- [x] Arrange-Act-Assert and Given-When-Then conventions with a lint-enforced naming scheme — one feature (`refundPolicy.ts`) tested twice, eight behaviours each, with `behaviours.ts` holding the exact title both sides must use so the comparison cannot quietly stop being one. The deliverable is the enforcement: a local flat-config plugin at `tdd/conventions/eslint-plugin/`. `title-scheme` runs over every test file in the repository — its default `behaviour` mode bans openers (modals, the runner's own name, meta-verbs) rather than prescribing a grammar, deliberately allowing a capitalised first word, since titles here legitimately open with `POST`, `Sulfuras` and `TypedTest.json()`; turning it on cost zero renames, because all 580 existing titles already stated what the system does and nothing was holding that. Under `scheme: 'given-when-then'` the same rule checks the sentence structure instead. `aaa-structure` requires the Act/Assert markers, in order, each occupied, and rejects any `expect(…)` before the `Assert` marker — the check that catches act-assert-act-assert bodies — and is enabled for `tdd/conventions/` only, since retrofitting markers onto the other 40 test files would rewrite every body in the repo to make a point about comments. `conventions.test.ts` runs the repository's *real* ESLint config over deliberately bad snippets, so a rule wired to nothing fails `pnpm test` rather than passing silently; emptying the config block and renaming a case were both verified to turn it red. The README's counts are parsed out of `gwt.test.ts` and became the honest argument against the shape it documents: 5 `Given` and 8 `When` blocks wrap 8 cases, and every `When` block holds exactly one. `engines.node` 22 floor moves to `^22.18.0` (Node resolves the plugin's TypeScript import itself) and `tsconfig.json` gains `noEmit` + `allowImportingTsExtensions`. 77 new tests; green on Node 22, 24 and 26 on run #1 (PR #30)
- [x] Characterisation tests for legacy code before refactoring — the Gilded Rose pins by hand, ten examples chosen by eye; `tdd/characterisation/` is the same move at a size where that stops working. One inherited invoicing function (`legacy/renewal.ts`: a mutable module-level tax table, `new Date()` and `Math.random()` inline, a write-back onto its own argument) whose documentation is wrong in five places, all five of them somebody's bill. The deliverable is the measurement rather than the tidier code: ten single behaviour changes are applied to the *real* source — string edits asserted to match exactly once, compiled and loaded through Node's own type stripping, so the mutants cannot rot away from the file they mutate — and three suites are run against all ten. A specification suite of 18 behaviours read carefully out of the docs stops 1; the golden master watching the returned invoice stops 8; the golden master watching every visible effect stops 10. Six of the ten changes are the code being brought into line with its own documentation, which is the point: every one would pass review. The specification suite's single catch is an accident — the docs say nothing about rounding, but asserting on a worked example means writing a number down, and 7.25% of 90 is 6.525. Corpus adequacy is a closed loop rather than a claim: 21 branch labels declared in the source, read back out of it by regex, and every one reached by a case. The recording carries a fingerprint over every input, so trimming the cases a change happens to break fails instead of passing. `seams.test.ts` substitutes the globals themselves to prove the one edit made before any test existed is a no-op when omitted, and `detection.test.ts` compiles the source unedited as a control first, because a pipeline that changed behaviour by itself would make every column look perfect. Four ways of cheating were each verified to turn the suite red. JSON cannot write down `-0` and the corpus produces one, so `observe.ts` normalises it and states the cost rather than hiding it. 101 new tests; green on Node 22, 24 and 26 on run #1 (PR #31)
- [x] The test pyramid vs the honeycomb: a documented ratio policy with CI enforcement — a ratio is only as honest as the thing it counts, and "unit test" is the vaguest word in testing, so `shape/` derives both halves instead of declaring either. A test's layer is the widest boundary it can *reach*, transitively: `boundaries.ts` is a closed table of every external module reachable from a test file, and an unclassified one fails rather than passing, because the alternative is silent and one-directional — add a dependency that opens a socket and every test reaching it keeps counting as a unit test, so the ratio improves on paper exactly when the suite gets slower. The gate caught that on itself; adding `@typescript-eslint/parser` failed `pnpm test` until it was classified. Keys are `module#Binding` where a package disagrees with itself: `eslint#ESLint` reads real config and paths, `eslint#RuleTester` lints strings in memory, and bare `eslint` is deliberately absent so a namespace import has to pick a side. Transitivity is load-bearing rather than decorative — `playwright/auth.spec.ts` reaches the browser through a barrel and a fixture, and a direct-import scan calls it a unit test. Counts come from `vitest list` and `playwright test --list`, not from the source, because a static count lands 19% low and unevenly: `RuleTester` generates 18 tests from no `it(` at all, `orderContract.test.ts` yields 28 from a contract invoked once per school, `k6/config.test.ts` builds 53 in a loop. Every one of those under-counts a file that is *also* integration, so a static census would have reported a tidier pyramid than the one that exists. Measured 61.5% unit / 32.8% integration / 5.7% e2e over 899 tests: the ordering is a pyramid, but it sits **outside** the textbook 10–30% middle band, and that is recorded as the finding rather than sized around — a pattern library that demonstrates boundary-crossing and audits itself by reading its own files has a legitimately fatter middle. Bands 55–80 / 15–40 / 2–10, with the e2e ceiling held at the textbook 10% because 12% let a doubled Playwright suite pass, which makes a ceiling decorative. Both headroom figures the PR first claimed were wrong and the tests found them: the 12% ceiling did not refuse a doubling, and the integration figure was computed as if bands did not interact — adding integration tests dilutes unit's share, so the unit floor binds at 401 while the integration ceiling would not bind until 402. Enforcement is split by cost: the classification runs in `pnpm test` (cheap, and the half with the silent failure mode), the ratio is `pnpm shape:check` as its own CI step, added to `GATED_SCRIPTS` so it cannot lose its `--throw-deprecation` block quietly. Verified to fail as well as pass — a 25% ceiling exits 1 naming the 32.8%. 93 new tests; green on Node 22, 24 and 26 on run #1 (PR #32)

Known gaps carried into Phase 8: the ratio counts tests, not seconds, and the
two come apart badly here — those 51 e2e declarations are 274 browser
executions across the six-project Playwright matrix, so a suite can satisfy
every band and still spend most of its wall clock at the top. A time budget is
the harder gate and is not built. Nothing in `shape/` says the tests are any
good; a pyramid of vacuous unit tests passes every check.
`pact/provider/users.provider.pact.verify.test.ts` collects zero tests unless
`PROVIDER_BASE_URL` is set and is the single entry in `EXPECTED_EMPTY`.

## Phase 8 — Advanced Correctness
- [x] Property-based testing with fast-check: invariants, shrinking, and custom arbitraries — twenty properties over one system (`property/availability.ts`, sets of half-open ranges) in three families, and the finding is that the predicates are the part that barely matters. Four of the five probes run the *same twenty invariants* and differ in nothing but the generator; they catch 4, 6, 7 and 10 of ten faults. `sparse` — integers across a decade of minutes, the choice a person makes without thinking — collides in 2.6% of scenarios, so the merging logic the system mostly consists of is barely executed while two hundred runs pass, and nothing in the output distinguishes uninformative from correct. `wide` overlaps *more* than the bounded arbitrary, not less, because `fc.double` draws across the bit-pattern space and comes back crowded with denormals near zero — and never produces two equal endpoints, so it is the one probe blind to `TOUCHING_NOT_MERGED`. `clustered` draws one origin and one grid spacing for the whole scenario and distributes them by `map`, which is what makes exact touching possible over floats. The textbook advice "build values, never filter them" is measured (46.4% yield naive, 1 in 1,000 for a sorted disjoint set, `.filter` silently paying ~1,000 draws per value where `fc.pre` fails outright) and so is its unadvertised cost: a generator that cannot produce an invalid range cannot produce a degenerate one, so three of the four arbitraries never reach `normalise`'s empty-dropping branch at all. Shrinking is measured against a real bug with `endOnFailure` as the control: 734 → 8 in 62 steps, reduced to `[[0, 3), [1, 2)]`, the minimal statement of the bug; the same values behind `fc.constantFrom` shrink 826 → 826 in zero steps. The example-suite comparison is reported as circular rather than as a result — 24 examples and 10 faults from one person's list agree by construction — and the measurement without that loop is `coverage.ts`: eight structural situations, twenty-eight pairs, counted over each probe's real inputs. The examples reach more *situations* than the bounded arbitrary and **one pair** in the whole corpus, which is what writing examples by hand is. Faults are flags on one shared implementation, not copies, and `faults.test.ts` pins the all-correct variant to `availability.ts` over 2,000 scenarios so the corpus cannot drift. `NUM_RUNS` 200 is measured at the edge (8/9/9/10 at 25/50/100/200) and `fast-check` is pinned exactly, because a seed only reproduces against the generator that produced it and every figure above is a property of that seed. 224 new tests; green on Node 22, 24 and 26 on run #1 (PR #33)
- [x] Mutation testing with Stryker across unit suites + a score gate in CI — coverage says a line ran and cannot say whether anything asserted on the result; `mutation/` measures the second thing over four modules (257 mutants, 87.55%, 2m54s) and gates on it. The scope is a closed table with a floor per module because mutation testing is minutes where every other gate here is seconds, and a gate nobody waits for acquires `continue-on-error: true`; `stryker.config.ts` derives `mutate` from that table and `policy.ts` fails a run for a module with no row *and* a row with no floor. Which **suites** run is derived instead, by `reach.ts` over the same import walk `shape/classify.ts` does — a scoped run's one way to report a badly wrong number is to load too few, since every mutant only the missing suite would have caught then reads as a survivor: the three suites a person would list for `property/availability.ts` score it 81.05%, the nine the graph finds score it 83.66%. What the floors actually catch was measured rather than assumed and is narrower than the folklore — deleting an assertion, a whole test, a whole behaviour from *both* suites testing it, or one of two redundant suites entirely each moved the score by **zero**, while eight lines of untested code took `factories/defineFactory.ts` from 100.00% to 58.33%, through its floor, with its *covered* score still reading 100.00% (which is why the gate uses the total). Two findings the suites could not state about themselves: over the same 153 mutants the 20 fast-check invariants kill 99 and the 24 hand-written examples kill 101, agreeing on 92 — the non-circular version of the comparison `property/README.md` reports as circular — and the AAA and GWT suites cover exactly the same 46 mutants with none to either, each scoring 96.00% alone, so a mutant cannot tell the conventions apart. Attribution is built on `coveredBy`, not `killedBy`, which names whichever test was scheduled first under bail; the complete kill sets cost 7m54s for one module against 2m54s for the whole gate. Three things had to be fixed that fail illegibly: Stryker's plugin glob comes back empty under pnpm and blames the install, the sandbox copy made this repository's own `vitest/` directory shadow the `vitest` package so every worker died in the dry run naming neither, and a sandbox left behind by a crashed run doubles the shape census, double-checks every file in `tsc`, and makes ESLint refuse to lint anything at all. Its own CI job on one Node major, since a mutation score is a property of the tests rather than the runtime; `mutation:check` joins `GATED_SCRIPTS`. Green on run #1 (PR #34)
- [x] Snapshot testing policy: what deserves a snapshot and how to stop rubber-stamping them — snapshot suites do not decay because snapshots catch too little; they catch more than anything else here. They decay because a snapshot over raw markup goes red for changes that broke nothing, so the headline is the **signal rate** — of the times a probe goes red, how often there is a bug — and not the detection rate the folklore reports. One subject (`render.ts`, an order summary in HTML), sixteen single edits applied to the real source and compiled through Node's type stripping: ten bugs and **six refactors that broke nothing**, which is the half that makes the number mean anything. The full-markup snapshot catches 10/10 bugs and fails on 6/6 refactors, for 62.5% signal; the projected snapshot and the fourteen hand-written assertions each catch 8/10 with no false alarm. Neither narrow probe contains the other, which is why the recommendation is a pair rather than a winner: the projection cannot see the badge's modifier class or the region's `aria-label` (markup carrying meaning without carrying a value, unreachable by any projection over `data-field`), and the assertions miss the discounted total and a tax of £0.998 where rounding decides the penny — real faults on the one order nobody wrote a case for, which is what an assertion suite's coverage is. Together: 10/10, no false alarms. The diff-size claim this started from was wrong and `yield.test.ts` killed it — over the eight bugs both snapshot probes catch the two mark *exactly* the same number of lines every time, because the fault changes a value and each document holds it once; what the projection changes is the document scanned (138 lines against 54) and the 64 lines of refactor diff that become zero. Diff size does not separate bugs from noise here, it inverts them: a re-indent marks 24 lines and seven of the ten real bugs mark six or fewer. Enforcement is a registry rather than a lint rule banning `toMatchSnapshot`, because that rule trades the only probe catching all ten for a tidier policy: every snapshot carries a row with a line budget and a sentence saying why it is a snapshot, closed in both directions like `mutation/scope.ts`. Six rules — unregistered, unused, over-budget, volatile, obsolete, empty — five decided from the filesystem inside `pnpm test`, with `obsolete` needing the collected test names (Vitest reports obsolete snapshots and exits zero) so `pnpm snapshot:check` spawns `vitest list` over just the files owning a `.snap` and joins `GATED_SCRIPTS`. Every rule was verified by causing its failure on this repository and restoring it. Four bugs in the inventory were caught by its own tests: a hardcoded repo root, an off-by-one measuring every inline snapshot one line long, `it.each` titles missed entirely, and a NUL byte that had made `policy.ts` binary to git. 91 new tests; green on Node 22, 24 and 26 on run #1 (PR #35)
- [x] Fuzz testing for parsers and validators — every guide to fuzzing is about the generator, and the generator is the easy half: a campaign produces two thousand inputs and then has to decide, for each one, whether the answer was right, with nobody looking. `fuzz/` measures the oracle instead. One subject — a strict JSON parser followed by a validator for one service's config schema, the shape every service has at its edge — with the split down the middle as the experiment: a parser implements somebody else's grammar, so `JSON.parse` is a free and near-exact oracle for it; a validator implements rules nobody else has, so no reference exists or could. Sixteen single-behaviour faults applied as edits to the real source, five probes over all of them. `crash` finds **1 of 16**, and every other probe finds that one too — a crash is not an oracle's verdict but the absence of one, so "we fuzzed it and it never crashed" is a claim about the runtime; the single probe that *misses* it is the strongest one, `differential`, whose list of declared divergences from `JSON.parse` tells it to skip the deep input that exposes a missing depth guard. `differential` takes 8 of 10 parser faults in four lines and **0 of 6** validator faults, which no effort would change. `roundtrip` takes 2, comparing a subject with itself. The two faults the whole apparatus misses are exactly the two that reject *valid* input: every oracle here states what the subject accepts, nothing generates a known-good input and demands acceptance, so over-rejection is invisible by construction and only a hand-written case catches it. The examples take 16/16 and that number is circular — same author as the faults — recorded as the strict-subset result it is rather than as a win. Generators are measured by reach against the subject's own closed outcome lists (13 parse refusals, 9 validation): uniform random reaches 7/13 and 1/9 and gets three documents in two thousand past the parser with zero accepted, schema-driven generation reaches 1/13 and 9/9, mutation over a corpus of real documents is the only single generator reaching both halves and still misses three, and a second table holding the *oracle* fixed names four faults each generator alone would miss. Three things had to be measured rather than reasoned about: the generator wrote `document['__proto__'] = value` to produce an unknown key, which sets a prototype instead — the same bug the corpus is measuring, committed by the fuzzer, silently removing the only input exposing `PROTOTYPE_POLLUTION`, and found by a `·` in the matrix rather than by anything failing; drawing each schema field independently produced a valid document 0.4% of the time, so every acceptance-side invariant was effectively untested until building a valid document and breaking one thing took it to 32%; and one fault survives 20,000 mutations of the corpus and falls at input 45 with a single seed added, which `seeds.test.ts` measures with everything else held identical and the seed deliberately still absent. The campaign does not run in CI — a search per commit is how a gate acquires `continue-on-error` — so `pnpm fuzz:record` runs it and commits one minimised witness per detection, `corpus.test.ts` replays them in milliseconds as the regression gate, and `detection.test.ts` re-runs the real campaigns and fails if the live matrix and the recorded one disagree, so the corpus cannot become a screenshot. Crash witnesses are deliberately not minimised: ddmin drives a stack overflow to the exact depth this runtime fails at (4,188 brackets) and that witness stops reproducing on a machine with a slightly larger stack. 394 new tests; green on Node 22, 24 and 26 on run #1 (PR #36)
- [x] Deterministic time and randomness: fake timers, injected clocks, seeded RNG — advice is usually "make the test deterministic" and it turns out to be five different things behind that one phrase. One subject (`session.ts`, an access-token issuer with a jittered background refresh) touches all five, and six strategies (`worlds.ts`) run thirteen shared behaviours against fifteen single-behaviour faults applied as edits to the real source. Every number is derived: reach is a capability subset, not a hand-written list; visibility is a 10,000-point grid sweep, not a random sample. The findings that fell out: seeding buys reproducibility not sight (0 cell change either way in the matrix), `vi.spyOn(Math, 'random').mockReturnValue(0.5)` is strictly worse than no control at all (9/15 vs 12/15, subset), and `JITTER_SIGN_FLIPPED` changes the delay on 99.99% of the draw space yet every random-draw strategy misses it — visibility in the input space is not the same thing as being asserted on. The two faults `standard` (fake timers + seed) still can't see are `ELAPSED_FROM_WALL_CLOCK` (needs the wall and monotonic clocks to move independently, which `fidelity.test.ts` measures as impossible under Vitest) and the sign flip. `audit.ts` / `registry.ts` closes the loop by pinning every ambient clock/draw/schedule read in the repository to a disposition; parser-based, since a regex over the codebase finds 19 sites outside `determinism/` and 4 are prose (a comment, two string literals, a test title containing `` `new Date()` `` in a sentence about `new Date()`) — a 21% false-positive rate would be a gate somebody switches off. One post-merge scale-up was needed: the first pass used TTL_MS=64 and Node 22/Node 24 CI runners had event-loop noise closing the 30ms gap between the fault and the correct delay; scaled 8× so the minimum correct delay is 64ms (all ratios preserved so no visibility number moved) (PR #37)
- [x] Concurrency testing: race detection and deterministic async scheduling — every other detection matrix here is boolean, and that is the wrong shape for a race: a strategy that catches one run in seven *does* catch it, and in a suite it is a test that goes red on somebody else's pull request and is quarantined by the end of the month with the bug still there. So every cell is a detection **rate** over independent trials, and `runsFor` turns it into the runs a gate would need before a green means anything. One subject (`ledger.ts`: a mutex, a read-modify-write, a check-then-act, an in-flight read coalescer), thirteen single-behaviour faults edited into the real source with a deliberate spread of *window sizes* — the same lost update appears twice, once with the lock removed and once with the critical section shortened by one statement — eight scenarios, twelve shared invariants, six strategies. `await Promise.all([...])` turns out not to be a search at all: every cell in that column is 0.000 or 1.000, because the microtask queue picks one interleaving and picks the same one forever, and it misses two faults on every run. A stress loop of 25 repeats takes a 15.0% detection to a measured 96.3% (98.3% predicted) and is still green on a real bug once every 27 runs. Drawing the schedule uniformly at random is *worse* than letting the runtime pick — it makes four faults flaky that `Promise.all` caught every time, including two where mutual exclusion is gone altogether, because a uniform draw spreads operations out and a lost update needs them bunched — and what it buys instead is a failure that comes back as seven integers and replays exactly. The finding that makes enumeration affordable is a property of the subject rather than the search: a lock that works leaves one store operation outstanding at a time, so the correct ledger's entire choice tree is **12 runs** against 383 once exclusion is broken — the interleaving space is created by the bug, and the run that costs most is the one about to find something. Two faults produce no answer rather than a wrong one, so no runner ever awaits the work directly: `runFree` awaits event-loop turns up to a budget, turning a deadlock into a named invariant failure in microseconds instead of a file timeout. `determinism/audit.ts` now counts `setImmediate` as a scheduler read (otherwise this directory walks past the gate the last item built) and its prose-vs-real-sites claim moves 15→16 real and 4→5 prose, the fifth being this directory's own paragraph about not drawing delays from `Math.random()`. Every rate is asserted against the live run and re-checked against the README, and all three held identically on Node 22, 24 and 26 — the engine-dependence the PR flagged did not materialise. Green on run #1 (PR #38)

## Phase 9 — Integration & Contracts
- [x] Testcontainers: Postgres, Redis, and Kafka spun up per suite with reuse — reuse does not break tests; state a suite does not own breaks tests, and a container per suite is a way of not having an isolation strategy and getting away with it. So the question measured is which of the things a suite already does about state is enough: three servers, three behaviours each chosen to trip on a different leftover, four strategies each, every suite run twice against one container, and all 36 cells re-derived on every run and checked against the README in both directions. The three stores fail in three different ways from the same cause — Postgres on its own assertion, the duplicate-email case in its *arrange* step so the trace points at a fixture, and Kafka not at all: a consumer group whose offsets sit at the end of the log receives nothing and goes on receiving nothing, so the suite is red only when an outer timeout fires, which is why the outcome vocabulary is `pass`/`fail`/`hang`. `TRUNCATE` is not a reset (a `bigserial`'s sequence is not a row, and two of the three Postgres behaviours pass under it, which is what makes it dangerous). A namespace is the only column complete for all three stores, and picking the wrong one is its own bug: a Redis key prefix does not partition `DBSIZE`, so it turns the one behaviour `none` *passes* into a failure, and a Kafka consumer group is only half the state. Deleting a Kafka topic is the only reset here that is not instantaneous — 63ms locally, 55ms in CI, and asynchronous, so it needs a polling loop `TRUNCATE` and `FLUSHDB` do not. `start()` resolving is not readiness and the gap is not where anybody would guess: Kafka's `start()` is the *fastest* of the three (225ms local / 144ms CI) and its readiness by far the slowest (+7,647ms across 146 probes, 97% of the wait), because `@testcontainers/kafka@12.1.0` passes `this.originalWaitStrategy` — `undefined` — into a parameter declared `waitStrategy = this.waitStrategy`, so the default fires and the post-script wait is the already-satisfied substitute log wait. Every store is therefore probed with one real operation of its own protocol before any test runs, and the testcontainers packages are pinned exactly because that finding is a property of a version. Reuse is measured (2,146→100ms, 207→18ms, 7,294→36ms) and is on locally, off under `CI`: a fresh runner's daemon has nothing to adopt while the liability stays — a reused container carries no session label, so Ryuk never reaps it, which `reuse.container.test.ts` checks directly and `pnpm containers:prune` cleans up. Two existing gates moved deliberately: `shape/`'s middle band 40%→45%, because `containers/` is a directory in which every test is a boundary test by subject matter and it took the measurement to 40.0% against a ceiling of 40 — the same pass re-derived every headroom figure and found the e2e ceiling has stopped binding anything at this suite size; and eleven `determinism/registry.ts` rows, all `measured`, with the parser-vs-regex claim moving 16→36 real sites and the false positives still at exactly five. One warning is recorded rather than fixed: kafkajs 2.2.4 schedules `throttledUntil - Date.now()` from an initial -1, so CI logs a `TimeoutNegativeWarning` that `--throw-deprecation` cannot see. 39 container tests; green on run #1 on both runs (PR #39)
- [x] Consumer-driven contract tests with Pact broker publish/verify in CI — the broker is not what catches breaking changes, and the corpus says so: over the eight scenarios where a provider edits its service and the contract does not move, all four wirings — a stale pact file, a fresh one, a broker, a broker with pending pacts — score *identically*, which is most of what "why you need a broker" material is about. Two things had to be fixed before any of it could run. The consumer suites had never executed: `MatchersV3.regex(...)` as a `Content-Type` value makes the FFI parse that header as a content type, fail, and `unwrap()`, so `pactffi_with_body` panics and takes the worker with it — no results, exit 1. And provider verification had never run anywhere, because the verify suite was a template pointed at `PROVIDER_BASE_URL` with six commented-out state handlers and there was no provider in the repository to point it at; `provider/app.ts` is one now, and `given('a valid refresh token exists')` turned out to be *unsatisfiable* without a parameter (the provider seeds a token it invented, the verifier replays the consumer's, and the 401 reads as a provider bug). Twelve scenarios, each stating whether it breaks the consumer **running in production**, so a false alarm costs a wiring what a miss costs: files-stale 7 caught / 2 missed / 1 false alarm, files-fresh 7/2/0, broker 8/1/0, broker-pending 8/1/0. The change every wiring misses is the one an ORM upgrade ships by itself — `"id": 1` becoming `"id": "1"` — and the wiring is not at fault: `MatchersV3.integer()` compiles to `{"match":"integer"}` and pact's matcher *coerces*, which `coercion.test.ts` proves rather than asserts by failing the same interaction with `"id": "abc"` through the provider's `bogusUserId` control. The two file columns catch equal numbers and do not contain each other, so "is the file current" is the wrong argument: staleness produces a miss *and* a false alarm, and copying more often trades one error for another. What the broker actually adds is `deployedOrReleased` — the two `LOGOUT_RETIRED*` rows are one retirement in its two possible orders, identical in every field but which consumer version is deployed, and `files-fresh` verifies *the current contract*, which is not the same document as *every contract still running in production*, so it scores the too-early retirement green. Pending pacts move exactly one cell, from the provider's build to the consumer's deploy gate: same detection, and the team interrupted is the one that caused it. One bug found while deriving it makes any file-based verification wrong on a machine configured for a broker — `pact-core`'s argument mapper reads `opts.pactBrokerUrl || process.env['PACT_BROKER_BASE_URL']` and adds a broker source if *either* is set, so `pactUrls` alone still fetches from the broker; it read as flakiness until `verify.ts` unset it and passed credentials explicitly. `http-proxy@1.18.1`, inside pact-js's state-handler proxy, is patched off `util._extend`: DEP0060 throws under the gates' `--throw-deprecation` *after* all 98 tests pass, which is how CI found what a local run without that flag could not. Green on run #1 after that fix, on Node 22, 24 and 26 plus a `contracts` job running the matrix against a real `pactfoundation/pact-broker` 2.143.0-pactbroker2.121.0 (PR #40)
- [x] OpenAPI schema-conformance tests asserting responses match the spec — "validate the body against the schema for this operation and status" is what the phrase almost always means, and it catches four of the eleven divergences in `openapi/drifts.ts`, because most of the ways a service leaves its document are outside the response body: an undeclared status, an undeclared content type, a missing required header, a query parameter the handler honours and the document has never heard of, where the body is impeccable and the schema has nothing to say. So six wirings are scored over twelve real exchanges: `status-only` 1/11, `handwritten` 3/11, `body-schema` 4/11, `body-schema-strict` 6/11, `full-response` 9/11, `request-and-response` 10/11. The jump is the envelope and not the dialect — registering `ajv-formats` and closing the document's open objects is worth two rows, while three checks that look at no schema at all are worth three — and the wirings are not a ladder: `status-only` catches a status the handler invented and `handwritten` catches a `text/plain` body, both of which the two schema wirings miss, so replacing hand-written assertions with schema validation can lose coverage. The row every wiring misses is `empty-collection`, which is the `wrong-type` bug against a filter that legitimately matches no rows: `[]` satisfies every item schema ever written, so conformance turns out to be a property of the responses a suite provoked rather than of the endpoint, and `coverage.ts` is the only gate here that notices the operation nothing ever called. The README states the matrix as a claim and the live run is checked against it. All seven checks green on run #1 (PR #42)
- [x] Database test isolation strategies compared: truncate, transaction rollback, template DB — the three the item names are not the choice, and both reasons only appear against a subject that opens its own transaction, which is what a service does and what every demonstration of this comparison avoids. **"Transaction rollback" is two strategies and only one isolates**: the `BEGIN`/`ROLLBACK` wrapper everybody describes is not an isolation strategy at all for such a subject, because Postgres has no nested `BEGIN` — the subject's `COMMIT` commits the harness's transaction, and the failure is not an error but two `WARNING`s on a channel most drivers do not surface, with the suite still green. The savepoint-rewriting version frameworks actually ship (Rails, Django, `pytest-django`) isolates correctly and costs five of ten faults and three of seven behaviours. Three measurements, each checked against `README.md` rather than generated from it: detection (8 strategies × (1 control + 10 faults) × 7 behaviours, 88 suite runs, each in its own database), leakage (the same probe twice, 40 pairs) and cost (serial, median of 20). The scoring is where the honesty lives — a fault counts as caught only when a behaviour was green on the correct subject and red with the fault, and without that subtraction `rollback-savepoint` scores 10/10 because its three unusable behaviours are red on every run, reversing the headline. Six columns score 10/10; `rollback` 9/10 and `rollback-savepoint` 5/10 — **the naive wrapper detects more precisely because it is not isolating anything**, catching all four commit-time faults for the same reason it leaks them. In the leakage table `rows` and `plain-insert` are identical but for whether the write went through a transaction, and `rollback` passes one and fails the other: the wrapper is not weak, it is conditional, and a team whose repositories are single-statement never sees it fail. `ids` is failed by `truncate` *and* `rollback-savepoint` — a sequence is not transactional, so `ROLLBACK` no more gives the number back than `TRUNCATE` does, and the two families usually described as opposites share that blind spot. Only the two namespace strategies are green everywhere, and the recommendation is `schema-per-test` (45.3ms) with `truncate-derived` (28.7ms) as the fallback: derive the table list, never write one down — `TRUNCATED_TABLES` omits `audit_log` on purpose and that omission is what a hand-written list becomes six months after somebody adds a table. The cost table's `connect-only` row is the point of it: "rollback is twenty times faster than truncating" is true (0.8ms vs 17.1ms) and is half a fact about a connection five of seven strategies open per test. Five Postgres facts are measured rather than cited, and one is in the list because the first draft had it backwards — a template database must have no other sessions connected and `STRATEGY` does not change that, `WAL_LOG` raising 55006 exactly as `FILE_COPY` does; the inference that the Postgres 15 default lifted the restriction is wrong and the shell probe that seemed to confirm it had a background session that had already exited. Two gates moved deliberately: `determinism/registry.ts` gains three rows with the parser-vs-regex claim at 36 → 45 real sites and the false positives still exactly five, and `shape/policy.ts`'s bands were re-drawn (unit 55→50, integration 45→48, e2e floor 2→1) because the repository grows by adding one boundary-measurement directory per item, so integration grows on almost every commit and unit does not — 48% rather than something rounder because `policy.test.ts` established that any middle ceiling at or above the unit floor is unreachable with the ordering intact, a band gate catching an edit to the bands. The ordering is now the claim and it reverses 263 integration tests out; when it does the honest move is to declare the honeycomb, not to widen a fourth time. 251 new tests; all seven checks green on run #1 (PR #43)
- [x] Idempotency and retry-safety test harness for HTTP handlers — the standard test for an idempotency key poses one hazard out of eleven, and it is the only one `memo-after` survives, so the implementation the phrase usually produces scores **4/11, exactly the control's score**, while passing every test anybody writes for it. Eleven hazards × eight designs, re-derived each run and checked against `README.md` in both directions, with each finding restated as an assertion in `matrix.test.ts`. `retry-backoff` — a real growing, seeded, jittered backoff on the injected clock — is *identical to `none` in all eleven cells*: backoff decides when a duplicate arrives and has no opinion about whether it is one, which is what the most common answer to "make our retries safe" is worth. `memo-after`'s 4 is not the control's 4 either: it fixes the sequential retry and adds two failures `none` lacks, an unscoped `WHERE key = ?` that hands one customer another's charge id and a memoised 502 that kills the key. The unfashionable design wins on cost — `natural-key`, one unique index and one `catch`, scores 9/11, beating every key-header design except `atomic-outbox` (10/11) — and `lease` (5/11) scores *below* `memo-before` (7/11), because a lease with no expiry is a lock with no owner and every crash becomes a permanent 409. Two structural findings the key table cannot reach: every strategy that has one loses `retry-after-decline` (storing the answer is the mechanism, and storing a failure with it makes the operation permanently impossible), and only the two designs passing a dedup key **downstream** survive `crash-after-gateway`, where `memo-before` and `lease` produce the table's only `orphaned` cells — money moved, no record. Three modelling choices are what make it a measurement rather than a demonstration: unique indexes that *block* (a `Map` has no concurrency, so the check-then-act that is the whole bug class is unreachable and every naive implementation passes), crashes that keep committed work and **release their locks** as a real database does for a dead connection (without which `crash-after-gateway`/`natural-key` deadlocks and reads as a flaky test), and a gateway that deduplicates only when given a key. Deterministic by construction — named park points released when the partner finishes or is observably blocked, injected clock, per-run ids, seeded LCG jitter, no timer or sleep anywhere — so `determinism/registry.ts` gained no rows. 172 new tests (119 unit / 53 integration), and `shape/` needed no band change: the split moves the measurement to 55.2% / 42.8% / 1.9%, with more headroom on both binding bands than before (PR #44)
- [x] Ephemeral per-PR environments with seeded data and automatic teardown — the clause that gets implemented is the last one, and the `pull_request: closed` workflow everybody ships scores **3 of 12**, one cell above a deploy job nobody automated and a human who is supposed to remember. Teardown is one of four things a preview has to get right and the only one anybody argues about; which commit is running, whose data the reviewer is looking at and who may make an environment at all are worth five of the eight cells between `on-close` and the top of the table. Twelve timelines × eight wirings, all 96 cells re-derived every run and checked against the README in both directions, every wiring the same runtime reading the same seven controls so adjacent rows differ by one field and `strategies.test.ts` asserts the adjacency rather than the prose claiming it. Runs are first-class — an event registers one, a `finish` step lands it, a `cancel` step kills it — which is what makes `zombie-deploy` writable at all: it is `finish` on the destroy before `finish` on the deploy, and the close hook it defeats is not wrong. **Adding a TTL sweep to a working close hook buys nothing**: `on-close-seeded` and `on-close-ttl` differ in one control and tie at 7/12, because the sweep moves three cells rather than zero — it rescues a cancelled teardown `leaked`→`reaped`, moves `platform-created-resource` `orphaned`→`reaped`, and turns `long-review` `live`→`broken` by deleting the environment under a reviewer who came back on day two. It trades a silent leak for a loud outage, which is a question about your team and not your infrastructure. The two wirings at 5/12 agree on two of five and fail in disjoint ways, so a league table calls equivalent two designs that are not close. `cancel-in-progress` is worth two cells and only with the group shared by the deploy *and* destroy workflows — `${{ github.workflow }}` in the group expression is that mitigation applied and not working — and one of the two is a teardown failure fixed by a deploy control. A **scrupulous** state file is still not a list of what exists: the deployer records everything it creates and deletes exactly that, and `platform-created-resource` reads `orphaned` against it because the platform made that resource on first use; an owned namespace fixes it without changing when teardown runs, and a label sweep only reaches `reaped`. The control beats six of the eight wirings on `fork-pr-unreviewed` by being too primitive to have the bug — `pull_request` has no secrets on a fork — and the two fork rows are one decision seen twice: `pull_request` is `clean` then `broken`, `pull_request_target` is `exposed` twice (still `exposed` after approval, because the secret ran at `opened`), and only a label gate passes both. **Nothing handles `sweep-never-ran`**: every recovering design rests on a `schedule:` workflow, which GitHub disables after sixty days of inactivity, quietly — the thing that bounds the loss is a spend alert, which is not a wiring and has no column. `teardown-cancelled` is correct at `reaped` rather than `clean` because nothing can undo a dead destroy run. `workflow-templates/preview-environment.yml` and `preview-reconcile.yml` are the top-scoring wiring as two copyable files, and `workflows.test.ts` derives the controls back out of them and compares with whatever row actually tops the matrix — no constant names the winner, so the templates fail the build rather than becoming last year's advice; five of seven controls are readable from a workflow file and the audit says which two are not instead of faking them. One existing gate moved deliberately: `mutation/reach.test.ts`'s whole-repository walk and the three like it in `scope.test.ts` now state a 60s timeout instead of inheriting Vitest's 5s default, after the first CI run timed out at 4.1s-under-contention locally and worse on a two-core runner — no assertion changed, and a `resolveLocal` cache was tried first and reverted for buying 5%. 196 new tests, deterministic by construction (no timer, sleep, randomness or wall clock), so `determinism/registry.ts` gained no rows and `shape/` needed no band change at 55.9% / 42.3% / 1.8%. All seven checks green (PR #45)

## Phase 10 — E2E Depth
- [x] Playwright component testing alongside full E2E — `ct/` mounts a component in Chromium with `@playwright/experimental-ct-react` and pairs each one with the same component under Testing Library, because the pairing is the argument: jsdom 26.1 reports every box as 0 wide and does not define `showModal` at all, so `OverflowTooltip`'s only branch is unreachable there and `ConfirmButton`'s dialog cannot be opened — and both jsdom answers are wrong in the direction that looks fine, since a tooltip that never appears and a dialog that never opens both pass a suite asserting their absence. Chromium: 568px of text in a 120px box, clipped by 448px; focus on Cancel, Escape restoring focus to the trigger, the backdrop intercepting the click that would reach it. A second Playwright config keeps the mount fixture off the application suite, `shape/` asks both configs (e2e 51 → 64 declarations, 274 → 287 executions) and classifies the browser-without-an-application boundary as `e2e` rather than inventing a fourth layer, and CI gets a required `component` job — the one Playwright suite this repository can run, since the application specs still have no application. `vite@^7` → `^8`, which ct-core requires (PR #46)
- [x] Network interception, HAR replay, and offline-mode simulation — the question every guide skips is what the fake *takes away*, and it is a question about a mechanism in a browser rather than about HTTP, so `intercept/` measures it: 8 wirings × 12 probes × 2 conditions, all 192 cells re-derived against Chromium every run against a fixture origin the config starts. **Eight of the sixteen rows came back the opposite of the first draft**, and the corrections are the content. Playwright matches a recorded entry on the *whole* request — a cache-busting query and a changed POST body are both misses — so no cell reads `echoes-recorded`: the feared quiet answer does not happen, at the price of a replay any varying payload defeats. The recording holds both answers the flaky endpoint gave, a 503 then a 200 for one URL, and every replaying wiring serves the 503 to **both** attempts, so a retry that recovers against the origin reports a bug against the recording. `abort-api` and `offline` agree on ten of twelve probes with no network behind them and differ on exactly `online-flag` and `offline-event` — the wiring almost every suite uses to "simulate offline" never tells the page, so an offline banner, a sync queue or a reconnect handler is never reached. `notFound: 'fallback'` and `notFound: 'abort'` are the *same wiring* under `severed` and differ under `connected` on exactly three probes, which is a list of the requests the default quietly answered over a socket. Five wirings answer the unchanged endpoint with a body stamped `origin` and one reached the origin: a replay is indistinguishable from the real thing by its bytes, and the only instrument that separates them is the origin's own ledger read outside the browser, which no real suite has — the README says so rather than pretending the stamp is realistic. `stub-fulfill` answers the flaky endpoint on the first attempt, so the retry branch is never entered while every assertion about the response still passes; `har-whole-page` and `offline-har` are the only wirings where the document loads at all without a network; and `setOffline` switches off the *connection*, not the routing, which is why `offline-har` is the one arrangement that can drive an offline-capable application. Three modelling choices make it a measurement rather than a demonstration: every JSON body is stamped with its source (the instrument, declared as such), the committed HAR is deliberately *stale* — `schema: 1` against an origin serving `schema: 2`, where an item changes from a string to an object — with `har.test.ts` failing if a re-record ever erases the two probes that depend on it, and "no network" is modelled by a catch-all abort registered *before* the wiring rather than by stopping the origin, so the specs can still ask it what it was asked. The README's two tables are not checked against `matrix.ts`, they are **rendered from it** and compared as text, and each of the eleven findings is a function over the table with the README required to carry exactly one paragraph per finding in both directions. 187 new tests (158 without a browser, 29 with one), deterministic by construction — no clock, sleep, randomness or timer — so `determinism/registry.ts` gained no rows, and `shape/` needed no band change at 56.1% / 40.9% / 3.0% with the e2e layer moving 2.2% → 3.0%. CI gets a required `intercept` job: the first suite here that needs a port as well as a browser. All nine checks green on run #1 (PR #47)
- [x] Cross-browser + mobile-emulation matrix with sharding — the five-project matrix everybody writes looks like three engines crossed with two form factors and is five cells: of Playwright's 207 device descriptors 200 are mobile and **none of the 200 is Firefox**, so the sixth cell cannot be written and a suite that believes it covers three engines on phones covers two. `matrix/` measures the rest against a real browser — 45 cells, three conditions, engine held still on purpose so no moved cell is ambiguous between the device and the engine. Each of the thirteen probes is filed as `viewport` or `device` *before* the run and the table partitions exactly along that line: a narrow window moves all three viewport probes and not one of the ten device probes. The one that matters is `hover-affordance` — a control hidden behind `:hover` inside `@media (hover: hover)`, the careful spelling — where a real phone reports `reachable` and the resize reports `hidden`, so a resize-only "mobile test" records the **opposite** value for the archetypal mobile bug and goes green; `page.tap()` is rejected outright without `hasTouch`, so it cannot even express the gesture. Two draft claims died against the measurement rather than the other way round: a descriptor's `screen` never reaches the page through a project's `use` (so `screen-width` is a viewport probe), and `fullyParallel: true` — the usual advice — is not monotone, improving the busiest shard at three shards (9 → 8) and making it worse at six (5 → 6) and eight (5 → 7), because finer groups move every later group's start index. On sharding, the unit is not the test: `shard.ts` reimplements Playwright's grouping and cut and `partition.test.ts` checks it against the real `--list --shard` over a 24-test fixture in seven shapes (64 assignments matched while it was written; five cases cover every branch). `fullyParallel` decides the unit, one `beforeAll` changes it again to chunks sized by `--shard`, the default config misses its own even cut at every count above two (`[8, 4, 10, 1]` against `[6, 6, 6, 5]`), over-sharding leaves a shard that runs nothing and exits 0, and a `dependencies` project is multiplied by the shard count rather than divided by it — eight shards, eight logins. `merge.test.ts` runs three real shards and merges them: drop one report and the merge says `unexpected: 0` about a smaller suite, which is why the merged total is what this directory asserts on. The whole sharding half needs no browser and rides `pnpm test` on every Node major; only the 45 emulation cells get their own job. CI found the one bug the local run could not: `JSON.parse(stdout)` on a channel that is not only the report's, now read from a file as `shape/collect.ts` always has (PR #48)
- [ ] Accessibility assertions inside E2E journeys (axe per page)
- [ ] Visual regression with masking, tolerance tuning, and a review workflow
- [ ] Trace-viewer-driven debugging guide for CI failures
- [ ] Auth state reuse via storage state with per-worker isolation

## Phase 11 — Performance & Reliability
- [ ] k6 scenarios: smoke, load, stress, soak, spike — with pass/fail thresholds
- [ ] Lighthouse CI budgets wired to a PR comment
- [ ] Flake detection: rerun analytics, quarantine automation, and a flakiness dashboard
- [ ] Test-impact analysis to run only suites affected by a diff
- [ ] Chaos testing: fault injection for latency, errors, and dependency outages
- [ ] CI test-time budget with a regression gate
