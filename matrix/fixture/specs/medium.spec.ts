/** Three tests, no hooks — a file large enough to straddle a shard boundary. */

import { test } from '@playwright/test'

test('medium-1', () => {})
test('medium-2', () => {})
test('medium-3', () => {})
